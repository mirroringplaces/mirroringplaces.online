const water_position = document.getElementById("water_position");
let rect = element.getBoundingClientRect("water_position");

let waterY = rect.y;
let = yellowGround;
let = redGround;

if (waterY = 0) {
    water_position.style.position = "relative";
    console.log(rect.y);
}